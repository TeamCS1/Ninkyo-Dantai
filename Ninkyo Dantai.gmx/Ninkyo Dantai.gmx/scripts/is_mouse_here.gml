/// is_mouse_here(x1,y1,x2,y2);

// Check if the mouse is over the area in question

if (mouse_x>argument0) && (mouse_x<argument2) && (mouse_y>argument1) && (mouse_y<argument3) {

    return 1;

} else {

    return 0;

}
