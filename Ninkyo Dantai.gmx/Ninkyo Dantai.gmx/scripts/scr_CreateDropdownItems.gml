///Dropdown Labels
    
for (i = 0; i < 9; i++) //the 100 controls how many total slots to display 
    {
        //Creates the slots
        with(instance_create(94,94 + (64 * i), obj_dropdown_slots))
        {   
           //Init them 
        }
    }

